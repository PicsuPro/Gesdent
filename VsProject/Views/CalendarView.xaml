<UserControl x:Class="VsProject.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VsProject.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             >
    <Grid>
        <!-- Header section -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Appointments,RelativeSource={RelativeSource AncestorType=local:CalendarView}}"
                  AutoGenerateColumns="False"
                  GridLinesVisibility="None"
                  CanUserAddRows="False">
            <DataGrid.RowHeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Margin="5" />
                </DataTemplate>
            </DataGrid.RowHeaderTemplate>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Monday" />
                <DataGridTextColumn Header="Tuesday" />
                <DataGridTextColumn Header="Wednesday"/>
                <DataGridTextColumn Header="Thursday" />
                <DataGridTextColumn Header="Friday" />
                <DataGridTextColumn Header="Saturday" />
                <DataGridTextColumn Header="Sunday" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>