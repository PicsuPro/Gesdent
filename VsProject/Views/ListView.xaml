<UserControl x:Class="VsProject.Views.ListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VsProject.Views" xmlns:viewModel="clr-namespace:VsProject.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <UserControl.DataContext>
        <viewModel:PatientListViewModel/>
    </UserControl.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="1" Background="White" 
                  Name="list"
                  ItemsSource="{Binding Patients}" 
                  IsReadOnly="True"/>
        <!--<StackPanel Grid.Row="2" HorizontalAlignment="Right">
            <TextBlock x:Name="numberPatient"
                       Text="Number Patient : 12" 
                       FontWeight="DemiBold"
                       Height="40"
                       Width="170"
                       FontSize="15"
                       Margin="15,2"
                       />
        </StackPanel>-->
    </Grid>
</UserControl>
