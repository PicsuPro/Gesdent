<UserControl x:Class="VsProject.Views.PatientListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VsProject.Views"
             xmlns:viewModel="clr-namespace:VsProject.ViewModels" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModel:PatientListViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Content="Add" Margin="50 20" Width="100" Click="Button_Click"/>
            <Button Content="Search" Margin="50 20" Width="100"/>
            <Button Content="Remove" Margin="50 20" Width="100"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Patients}" IsReadOnly="True" AutoGenerateColumns="True"/>

        <Popup x:Name="PatientEditView" 
               StaysOpen="False" 
               IsOpen="False"  
               Width="Auto" 
               Height="Auto">
            <local:PatientEditView/>
        </Popup>
    </Grid>

</UserControl>
